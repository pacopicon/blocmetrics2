<%= content_tag :div, class: 'media', id: "event-#{event.id}" do %>
<!-- or: <div class="media" id="event-<%= event.id %>"> -->
tracking <strong><%= event.name %></strong>
<% end %>


# this goes in regapp#show:

<%= render partial: 'events/event', collection: @events %>
